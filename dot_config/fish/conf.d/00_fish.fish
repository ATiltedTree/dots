# Configuration for fish

# Disables greeting
set -U fish_greeting ""

# Adds ~/.local/bin and ~/.local/share/cargo/bin to the PATH
set -U fish_user_paths $HOME/.local/bin $HOME/.local/share/cargo/bin
